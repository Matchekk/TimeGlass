{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for TimeGlass",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "sql:default"
  ]
}
